import type { Metadata } from "next";
import { Playfair_Display, Inter } from "next/font/google";
import "./globals.css";

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
  display: "swap",
  weight: ["400", "500", "600", "700"],
});

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
  weight: ["300", "400", "500", "600", "700"],
});

export const metadata: Metadata = {
  title: "Prime Tiles - Premium Ceramic & Vitrified Tiles",
  description: "Prime Tiles - Premium ceramic and vitrified tiles for every space. Explore our extensive collection of wall tiles, floor tiles, and designer tiles.",
  keywords: "tiles, ceramic tiles, vitrified tiles, floor tiles, wall tiles, bathroom tiles, kitchen tiles",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${playfair.variable} ${inter.variable}`}>
        {children}
      </body>
    </html>
  );
}
